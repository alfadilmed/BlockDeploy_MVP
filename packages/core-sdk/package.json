{
  "name": "@blockdeploy/core-sdk",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -w -p tsconfig.build.json",
    "lint": "eslint . --ext .ts,.tsx",
    "test": "echo \"Error: no test specified yet\" && exit 0"
  },
  "dependencies": {
    "ethers": "^6.11.1", // ethers v6
    "wagmi": "^2.5.7",
    "@tanstack/react-query": "^5.28.6" // wagmi a react-query comme peer dependency
    // WalletConnect v2 (via wagmi connectors)
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20",
    "eslint": "^8",
    // ESLint plugins seront hérités de la racine ou peuvent être ajoutés ici si besoin
    "react": "^18", // Peer dependency pour wagmi
    "@types/react": "^18" // Peer dependency pour wagmi
  },
  "peerDependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  }
}
